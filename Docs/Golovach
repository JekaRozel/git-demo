	Структурное программирование
конструкци процедурного кода
рекурсия
динамические структуры данных
 теорема структурного программирования: любая прогармма может быть переписана с использованием всег 3-х конструкций 1)следование (последовательные инструкции, строка за строкой) 2)цикл 3)логическое ветвление.
 принцип наименьшего удивления - не удивиться поняв как работает код (без сюрпризов)
 
СОРТИРОВКИ:
a=a+b;
b=a-b;
a=b-a;


java.util.Arrays.binarySearch;(массив, элементКоторыйПроверяем) он ищет положения значения в массиве. если такого элемента нет, то он сам выбирает для него место. Если выводит положительное число, то это номер элемента в массиве, если 0 и меньше, то предлагает вставить на номер "с положительным знаком минус один" (-1 -> 0: -4 -> 3)

копирование куска массива в другой(или в себя же) массив осуществляет метод system.arraycopy(1массив, положениеОткудаБерём, 2массив, положениеКудаВставляем, кол-во элементов);
(mas1, 2, mas2, 4, 6);

нативный метод native; без тела, выглядит как абстрактный, но это значит что он реализован на другом языке программирования

		Видео Головач
Память stack heap permanentGeneration
соотношение их размеров зависит от версии JVM
 stack. состоит из фрeймов (стандартный размер 1Мб) (каждый фиксированного размера под каждый поток). Фрэйм хранит локальные переменные и методы классов.  
